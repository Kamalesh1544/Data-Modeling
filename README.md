# Data-Modeling
 Production-grade FastAPI backend demonstrating PostgreSQL (3NF), MongoDB (document), and Neo4j (graph) for the same B2B e-commerce domain. Features Firebase auth, async task processing via Taskiq/Redis, Docker Compose orchestration, and 15 REST endpoints with InjectQ dependency injection.
